# Dependencies for CI data fetching and chart generation.
# Excludes PySTAN (stan) only -- geopandas/shapely/joblib/scipy are needed by the chart pipeline
# via the NECIR_CSO_map → EEA_DP_CSO_map → dashboard_charts import chain.

# HTTP / scraping
requests==2.33.1
beautifulsoup4==4.14.3
lxml==6.0.2
unidecode==1.4.0

# Data
pandas==3.0.2
numpy==2.4.4
openpyxl==3.1.5
sqlalchemy==2.0.48

# APIs
sodapy==2.2.0
us==3.2.0

# Visualization (charts + maps)
matplotlib==3.10.8
folium==0.20.0
plotly==6.6.0

# Geospatial (required by NECIR_CSO_map → EEA_DP_CSO_map import chain)
geopandas==1.1.3
shapely==2.1.2
pyproj==3.6.0
fiona==1.10b2

# Numerical / stats (scipy used inline in MADEP scripts; joblib used by NECIR_CSO_map)
scipy==1.17.1
joblib==1.2.0

# PDF extraction + AI (used by get_MS4_annual_reports.py; manual-run only, not in CI pipeline)
pdfplumber==0.11.9
google-genai==1.73.1

# Misc
six==1.17.0

# Testing
pytest==9.0.3
pyyaml==6.0.2
